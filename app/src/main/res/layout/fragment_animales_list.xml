<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="#F5F5F5"
    tools:context=".ui.admin.AnimalesListFragment">

    <!-- Título de la sección -->
    <TextView
        android:id="@+id/tv_section_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Galería de Animales"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#1976D2"
        android:layout_marginTop="8dp"
        android:layout_marginStart="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- CONTENEDOR DE SCROLL HORIZONTAL PARA LAS LEYENDAS (Soluciona desborde) -->
    <HorizontalScrollView
        android:id="@+id/horizontal_scroll_legend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/tv_section_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:paddingHorizontal="8dp">

        <LinearLayout
            android:id="@+id/legend_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Leyenda: "
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="#666666"/>

            <!-- Item 1: Disponible Adopción (Rojo/Naranja fuerte) -->
            <View
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/legend_dot_red"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="Disponible Adopción"
                android:textSize="11sp"
                android:textColor="#666666"/>

            <!-- Item 2: Medicación Activa (Naranja/Amarillo) -->
            <View
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_marginStart="12dp"
                android:background="@drawable/legend_dot_orange"/>
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="Medicación Activa"
                android:textSize="11sp"
                android:textColor="#666666"/>
        </LinearLayout>
    </HorizontalScrollView>
    <!-- FIN DEL CONTENEDOR DE SCROLL HORIZONTAL -->

    <ProgressBar
        android:id="@+id/progress_bar_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- RecyclerView para la lista de tarjetas (RF-06) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_animales"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_constraintTop_toBottomOf="@+id/horizontal_scroll_legend"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_animal_card" />

    <!-- Botón Flotante para Agregar Nuevo Animal (FAB - RF-05) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_animal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add"
        app:backgroundTint="#388E3C"
        app:tint="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Agregar nuevo animal rescatado" />

</androidx.constraintlayout.widget.ConstraintLayout>