package com.refugio.pawrescue.ui.volunteer;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;
import com.refugio.pawrescue.R;

/**
 * Activity Principal para el rol de Voluntario.
 * Placeholder que será desarrollado en fases posteriores (RF-04).
 */
public class VolunteerMainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_volunteer_main);
    }
}